import React from 'react';
import { FormattedMessage } from 'react-intl';
import '../../styles/components/end-list.scss';

const EndList = () => (
  <div className="end-list" >
    <FormattedMessage id="list.endlist" />
  </div>
);

export default EndList;
